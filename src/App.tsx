import { styled } from '@mui/material';

function App() {
  return <Main>Hello World</Main>;
}

const Main = styled('main')({
  width: '100%',
  height: '100dvh',
  position: 'relative',
});

export default App;
